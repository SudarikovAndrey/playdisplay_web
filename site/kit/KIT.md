# Конструктор презентационных страниц PLAYDISPLAY

Набор блоков, из которых собрана книга «Музей Мяча», плюс приёмы лендинга. Нужен для
одного: собрать новую презентационную страницу за час, а не за неделю, и не переоткрывать
задачи, которые уже решены. Всё, что однажды ломалось, записано в поле `traps` описи —
это не украшение, это самая ценная часть.

## С чего начать чату, который собирает новую страницу

1. Прочитать `blocks.json` — опись блоков. Там у каждого: зачем он, какая нужна разметка,
   какие файлы подключить, какие есть опции и на чём люди спотыкались.
2. Открыть живой каталог: `python3 serve.py` в корне проекта и
   `http://localhost:8000/kit/catalog.html`. В каталоге каждый блок **отрендерен**, а рядом
   показана **его же разметка** — она печатается из самой демонстрации, поэтому не может
   разойтись с видом.
3. Создать скелет страницы: `python3 site/kit/new-page.py имя-страницы "Заголовок"`.
   Скелет уже подключает кит в правильном порядке и содержит первый экран, главу и подвал.
4. Собирать страницу из блоков каталога. Своё писать только тогда, когда подходящего блока
   нет — и тогда сразу дописать его в кит (см. ниже).

## Что где лежит

```
site/kit/
  KIT.md          ← этот файл: принципы и порядок работы
  blocks.json     ← опись блоков: зачем, разметка, опции, подводные камни
  catalog.html    ← живой каталог: блоки видно и код рядом
  new-page.py     ← скелет новой страницы (умеет и закрытую по паролю)
  css/            ← layout · typography · components · animations · scrub
  js/             ← boot · reveal-cursor · nav · parallax · lists · hotspots
                    xray · courts · ball · ui · scrub · catalog
  parts/          ← фрагменты разметки для сборки страниц
  assets/demo/    ← лёгкие примеры для каталога (кадр, рентген-слой, ролик, портрет)
```

**Порядок подключения важен.** CSS: `layout → typography → components → animations`
(и `scrub.css`, если нужна крутилка). JS с `defer`:
`boot → reveal-cursor → nav → parallax → lists → hotspots → xray → courts → ball → ui`
(и `scrub`). Порядок не случайный: `layout` задаёт токены и сетку, на которые опираются
остальные файлы; `boot` должен успеть закрыть страницу до того, как остальное начнёт
рисовать.

## Как расширять кит

Это не библиотека «на почитать», а живой инструмент. Правило одно:

> **Придумали новый блок или приём — сразу положите его в кит.**
> CSS/JS в `kit/`, запись в `blocks.json`, демонстрацию в `catalog.html`.
> Иначе следующий чат изобретёт то же самое заново и сделает иначе.

Демонстрация добавляется так: в `catalog.html` внутри `<template id="kbDemos">` положить
`<div data-block="ваш-id"> …настоящая разметка… </div>`. Каталог сам напечатает её в блоке
«Разметка» и сам посчитает, не осталось ли блоков без демо — строка состояния наверху
показывает расхождение честно.

Если блок живёт только на своей странице (как WebGL-переход лендинга), запись в описи
всё равно нужна: с полем `inCatalog: false` и указанием, где смотреть живьём. Знать о
существовании приёма важнее, чем иметь его копию.

## Принципы, на которых собран этот кит

- **Комментарий объясняет ПОЧЕМУ, а не что.** «Здесь fillRect, потому что круги в кадр не
  укладывались» — полезно. «Рисуем прямоугольник» — нет.
- **Числа в оформлении проверяются замером.** Порог шва — долей площади, зазор — пикселями,
  габарит — измерением в браузере. На глаз ошибиться легко: порог футбольного шва, взятый
  «на глаз», отдавал шву 70 % шара.
- **Правку видно на картинке.** Прежде чем сказать «готово», посмотрите: скриншот, замер
  геометрии в браузере, прогон кода в node с выводом в PNG. Половина ошибок в этом ките
  найдена именно так, а не чтением кода.
- **Сначала причина, потом настройка.** Если что-то выглядит не так, ищите, почему так
  выходит. Разметка площадок «складывалась не в ту сторону» не из-за коэффициента, а из-за
  того, что камера стояла за той гранью.
- **Одна задача — один проход.** Это про модель в ассистенте, но верно и здесь: не
  смешивайте в одном изменении содержание и оформление.

## Правила набора текста

- **Длинное тире (—) не используем никогда.** Только обычное короткое тире с пробелами:
  «LADA – не просто машина, а мечта». Длинное тире в узкой колонке читается как
  разделитель блоков, а не как знак внутри фразы, и рвёт строку пополам.
- **Висяки в заголовках недопустимы.** Союз, предлог или частица не остаются в конце
  строки: приклеиваем их к следующему слову неразрывным пробелом (`&nbsp;`). Заголовок
  должен ломаться по смыслу, а не по случайной ширине колонки.
- Проверять на ДВУХ ширинах: на широком экране висяк может не появиться, а на 1280 —
  появиться.

## Управление документом

- **Кнопка «во весь экран» стоит В ШАПКЕ**, рядом с меткой документа, а не в нижнем углу:
  там уже живёт «в начало», и две кнопки в одном углу читаются как пара равных действий,
  хотя одна двигает по документу, а другая меняет режим показа. Разметка и подводные
  камни — блок `fullscreen` в описи.
- **Состояние полноэкранного режима читаем из события `fullscreenchange`**, а не из своей
  переменной: выйти можно клавишей Esc мимо кнопки, и подпись разойдётся с реальностью.
- **«В начало» в режиме слайдов прыгает мгновенно.** Плавная прокрутка к нулю борется с
  прилипанием: браузер цепляется за ближайшую точку привязки и оставляет страницу на
  середине.

## Аналитика

Счётчики живут в ОДНОМ файле — `site/analytics.js` (Метрика 111509723 + GA4
G-8L3XFCPG2V). В ките своей копии нет сознательно: две копии — это два номера счётчика,
которые однажды разойдутся. Новая страница подключает файл одной строкой в `<head>`,
и скелет из `new-page.py` уже делает это сам:

```html
<script src="/analytics.js" defer></script>
```

Путь абсолютный: страница может лежать на любой глубине. Что важно знать:

- **Считаются только боевые домены** (проверка по `location.hostname`). Локальный
  просмотр и превью в статистику не попадают — иначе свои же проверки выглядели бы
  в отчётах как посетители.
- **Do Not Track уважается**: при включённом DNT не создаются ни счётчики, ни слушатели.
  Это обещание, данное человеку на `privacy.html`, и оно должно быть правдой.
- **Внутрь iframe-сцен подключать нельзя** — визит посчитается дважды. Файл и сам
  выходит, если он не в верхнем окне, но лучше не подключать вовсе.
- **Цели отправляются одной строкой**: `pdGoal('название')` — уходит и в Метрику, и в GA4.
  Уже настроены `ai_open`, `ai_start`, `brief_send`, `book_send`, `book_click`,
  `contact_click`, `generate_lead`. Клики и отправки форм лендинга файл ловит сам,
  правок в разметке для этого не нужно.
- **В каталог кита аналитику не ставим**: это инструмент, его посещения — мусор в отчётах.

## Правила общей папки

- Книгу `site/ball/private/page.html` **не править**: это выпущенный документ. Кит живёт
  отдельно и развивается свободно.
- Лендинг `site/index.html` — чужая территория, там работают другие чаты. Приёмы оттуда
  переносим в кит копией, сам лендинг не трогаем.
- Перед правкой — `git status`. В папке одновременно работают несколько чатов.
- Коммит — по именам файлов, никогда `git add -A`.
- Деплой запускает владелец: `./deploy.sh "что сделали"`. Из чата до хостинга не дотянуться.

## Что уже есть, коротко

Каркас: заставка, шапка с логотипом по сетке, полоса разделов, полоса прочитанного,
кнопка «в начало», мягкая прокрутка, запрет выделения, фоновый звук.
Курсор: точка со свечением и частицами; мяч из точек в заданных зонах.
Первый экран: слои параллакса, облако точек с шестью мячами, кадр во весь экран,
рентген-просветка структуры.
Текст: главы, крупная фраза построчно, живые списки, врезка, разметка площадок на фоне.
Медиа: кадр с подписью, вылет во всю ширину, ролик-иллюстрация, подсказки на кадрах,
крутилка кадров ролика, встройка 3D-сцены, WebGL-переход.
Диаграммы: лепестковая по числам, плашки с числами, цепочка шагов, карточки идей, путь
посетителя, коллекция, финал с портретом.
Коммерческое предложение: пропуск под ещё не пришедшее число, сопоставление «как обычно /
как у нас», плашка предложения с ценой. Плюс модификатор `.chapter--wide` — глава, собранная
из карточек и плашек, а не из абзацев.
Доказательство и партнёры: полоса фактов и кадр приложения на первом экране, связка знаков
«А × Б» с подписанным местом под недостающий логотип, полоса экранов приложения.
Темы: `theme-street.css` — «асфальт и картон», оформление под мир игры (включается классом
`theme-street` на `body`, фактуру картона даёт страница переменной `--kraft`).
Встроенная 3D-сцена: тяжёлая сцена подключается отдельной страницей через iframe.
Растворение переднего плана (`js/scene-dissolve.js`): всё между камерой и предметом
рассыпается в пыль — предмет можно крутить внутри помещения на все 360°.
Режим презентации: `deck` на `<html>` — страница листается экранами, один посыл на слайд;
крупная мысль и пояснение стоят на одном экране. Знаки площадок — ряд плашек «где играют».

Точный список и все подводные камни — в `blocks.json` и в каталоге.

## Что чинилось в ките и почему это важно помнить

- **Кегель на телефоне задаёт самое длинное слово, а не вкус.** Крупный шрифт не переносится
  внутри слова: блок не может стать уже своего длинного слова и вылезает за экран вместе со
  всей страницей. Замер на экране 401 px: `h1` при 23vw давал «ПАЦАНА» 502 px (вылет 141),
  крупная фраза при 13vw — «заканчивается» 485 px. Сейчас 15vw и 10.5vw, вылет ноль.
  Проверка простая: свернуть окно до 400 px и сравнить `scrollWidth` с `clientWidth`.
- **`1fr` в сетке — это `minmax(auto, 1fr)`**, то есть снизу он ограничен шириной содержимого.
  Длинная подпись в плитке раздвигала полосу за экран. Везде, где колонки должны делиться
  поровну, стоит `minmax(0, 1fr)`.
- **Правила внутри `.chapter-copy` сильнее одиночного класса блока.** `.chapter-copy p`
  обнуляло `margin: auto` у цены, и в двух карточках разной длины она расходилась по высоте
  на 24 px. Блокам, живущим внутри главы, нужна специфичность из двух классов.
- Все три ошибки нашлись ЗАМЕРОМ в браузере, а не чтением кода. Так и проверять.

## Живая 3D-сцена: что выяснилось на гараже с NIVA (16.08.2026)

`site/avtovaz/garage.html` — конфигуратор: модель крутится, красится, поднимается на
подвеске, колёса выворачиваются. Всё это на модели, которая пришла ОДНОЙ сеткой без
разделения на детали. Что стоило времени:

- **Модель из Meshy весит 63 МБ и содержит 1,95 млн треугольников.** В веб она едет только
  после `gltf-transform optimize --simplify-ratio 0.045 --texture-size 1024`: 3,7 МБ и
  88 тысяч треугольников, разница на глаз незаметна.
- **`--compress meshopt` не включать**, если грузите штатным GLTFLoader r128 без декодера.
- **Чересстрочная раскладка вершин ломает наивную выборку.** После оптимизации атрибуты
  лежат в общем буфере (interleaved), и чтение `attribute.array[i * itemSize]` даёт кашу из
  осколков. Читать только через `getX/getY/getZ`.
- **Колёса выделяются из общей сетки** цилиндрами у нижних углов габарита, центры считаются
  в два прохода (второй — уточнение по точкам внутри первого цилиндра). За один проход в
  «колесо» уезжала половина бампера.
- **Перекраска запечённой текстуры маской по насыщенности или светлоте НЕ РАБОТАЕТ.** Замер
  гистограммы прямо в браузере: 99 % текселей с насыщенностью ниже 0.1, 94 % — в полосе
  светлот 0.1–0.3. Кузов от резины по картинке не отличить; отделять надо геометрией, а
  краску вешать только на материал кузова.
- **Главная ловушка: `metalness = 1` у импортированной модели.** У полного металла нет
  диффузной составляющей, и кузов показывает только отражения ламп — краска не видна вовсе,
  сколько ни правь шейдер. Крашеный кузов — это `metalness ≈ 0.2`, `roughness ≈ 0.42`.
- **Колесо мыши внутри сцены перехватывать нельзя** (только с Shift): иначе рамка съедает
  прокрутку и документ застревает на сцене.

## Режим слайдов: что пришлось узнать (16.08.2026)

- **Класс `deck` вешается на `<html>`, а не на `<body>`.** `scroll-snap-type` с тела к
  области просмотра не поднимается (в отличие от `overflow`) — прилипание молча не работает.
- **Жёсткое прилипание (`mandatory`) допустимо, только пока КАЖДЫЙ слайд помещается в
  экран.** Иначе низ высокого слайда становится труднодоступным. Проверка — замером высот:
  на 1440×900 из двадцати слайдов пять были выше экрана, чинились не прилипанием, а самим
  слайдом (восемь шагов в две колонки, сцена ниже, отступы меньше).
- **На телефоне прилипание переведено в `proximity`:** при 402×820 тринадцать слайдов из
  двадцати выше экрана, потому что карточки встают в столбик.
- **Свою мягкую прокрутку пришлось выключить** (`js/ui.js` проверяет `html.deck`): перехват
  колеса и прилипание — два механизма на одну прокрутку, экран останавливался между слайдами.
- **Крупная мысль и пояснение — на одном слайде.** Разнесённые по двум экранам они дают
  между собой пустой экран, и презентация читается как набор дыр.

## Как забрать картинки из Figma (16.08.2026)

Плагин **Talk To Figma** плюс сокет-сервер: `bunx cursor-talk-to-figma-socket` (или
`npx -y cursor-talk-to-figma-socket`), в плагине нажать Connect и передать агенту код
канала. Дальше `join_channel` — и доступны `get_document_info`, `scan_nodes_by_types`,
`export_node_as_image`.

**Ловушка: MCP-инструмент экспорта отдаёт картинку В ПЕРЕПИСКУ, а не на диск** — сохранить
файл из такого ответа нельзя. Обходится своим клиентом к тому же сокету (node, глобальный
`WebSocket`): шлём плагину `export_node_as_image`, получаем `message.result.imageData`
в base64 и пишем в файл. Скрипт — `scratchpad/figma_pull.mjs`.
Картинкой считать ТОЛЬКО ответ с полем `imageData`: сервер шлёт ещё и системные строки
(«Connected to channel…»), и первая версия скрипта записала одну из них как «картинку»
на 19 байт.

**Зачем это нужно было:** в макете лежали ГОРИЗОНТАЛЬНЫЕ кадры игры 1920×1080, а в сторах
только вертикальные 1282×2778. Вертикальный кадр, растянутый на широкий слайд, увеличивается
почти вдвое и выглядит мыльным — см. блок `slide--cover` и его модификатор `--cover-wide`.

## Качество 3D: чем жать, а чем не жать (16.08.2026)

Первая сборка гаража жала всё подряд `optimize --simplify-ratio 0.045` — модель ужалась до
3,7 МБ, но кузов «поплыл», а текстуры превратились в кашу. Правило простое:

- **Геометрию НЕ жать до неузнаваемости.** Для модели уровня «рассмотреть машину» 0.22 от
  исходных 1,95 млн треугольников (428 тысяч) — предел, ниже которого видно грани.
- **Вес почти всегда в текстурах, а не в сетке.** У правильно собранной модели (кузов и
  четыре колеса отдельными узлами, 28 + 9 тысяч треугольников) 37 МБ приходились на шесть
  PNG 4K. `resize --width 2048` + `jpeg --formats "*" --quality 92` дают 8,6 МБ БЕЗ потери
  геометрии и без видимой потери качества.
- **`gltf-transform jpeg` по умолчанию не трогает PNG:** ключ `--formats` по умолчанию
  «jpeg», то есть пережимает только уже сжатое. Нужен `--formats "*"`.
- **Свет: одного окружения мало.** IBL даёт объём и отражения, но не даёт направления —
  ни блика по ребру, ни тени на полу, картинка выходит пресной. Нужен ключевой источник
  с мягкой тенью (карта 2048, рамка тени по габариту предмета, а не по комнате), холодный
  контровой сзади и тёплая подсветка снизу-спереди.
- **Стены, которые растворяются, не должны отбрасывать тень:** они то есть, то нет, и тень
  от них мигает. `castShadow = false`, `receiveShadow = true`.
