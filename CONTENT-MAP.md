# Где что лежит — карта проекта Web_Playdisplay

Разбор и уборка: 27.07.2026. Всё проверено по факту — файлы на диске, ветки git, история, живая страница.

---

## Как устроено сейчас

```
Web_Playdisplay/
  site/                     ← САЙТ. 137 МБ, 284 файла. Единственный источник правды
    index.html              ← лендинг-SPA целиком: стили, CASES, сетка Work, кейс-вьюер, FlyGL, роутинг
    hero-scene.html         ← 3D-hero, встраивается iframe'ом
    data/
      projects.json         ← 15 кейсов: цели, решения, flow-блоки, медиа
      concepts.json         ← библиотека авторских концепций
      atlas.json            ← Атлас: 90 карточек в шести типах
    assets/  videos/        ← картинки и сжатые ролики
    work/<slug>/            ← статические SEO-страницы кейсов (генерируются)
    model.js  odk_frame.js  textures.js  ← 3D-данные для hero
    sitemap.xml  robots.txt  llms.txt    ← генерируются

  build_seo.py              ← генератор /work/<slug>/, sitemap, robots, llms, SEO-блока главной
  download_assets.sh        ← докачка картинок проектов
  CLAUDE.md                 ← паспорт проекта: деплой, что не ломать
  CONTENT-MAP.md            ← этот файл
  README.md
  .github/workflows/        ← деплой (см. «Что осталось решить»)

  _ARCHIVE/                 ← ВНЕ GIT. Всё старое, «на всякий случай». См. _ARCHIVE/README.md
```

**`site/` самодостаточна** — проверено: ссылок за свои пределы не имеет. Всё, что нужно сайту, лежит внутри неё.

Точка возврата в git: тег **`archive/before-cleanup`** — состояние до уборки, все файлы на прежних местах.

---

## Что было не так и куда это уехало

Причина путаницы: **три поколения сайта лежали в одной папке**, и все три отслеживались git.

| Что | Было | Стало |
|---|---|---|
| Старый сайт «Бренд-платформа 2026» | корень: `index.html`, `css/`, `js/`, `assets/`, `about|contact|projects.html` — 12 МБ, 24 файла в git | `_ARCHIVE/1 - старый сайт 2026/` |
| Сборка 3D-прототипа | `dist/` — 91 МБ, 13 файлов в git | `_ARCHIVE/2 - прототип 3D/` |
| Исходники 3D-сцены | `proto 3d/` — `.blend`, `.fbx`. Был прописан в `.gitignore`, но игнор не действует на уже отслеживаемые файлы | `_ARCHIVE/2 - прототип 3D/` |
| Слепки `site/` — 490 МБ, уникального ничего | `dist_upload/` (копия от 21 июля), `site.zip`, `dist.zip`, `ziVRGV2O` (дубль `pdf_images_VMF.zip`) | `_ARCHIVE/3 - копии текущего сайта/` |
| Сырьё и инструменты — 571 МБ | `videos/` (мастера), `concepts raw/`, `remade/`, `picture converter/`, `pdf-slides-extractor/` | `_ARCHIVE/4 - рабочие материалы/` |
| Документы чужого проекта | два `.docx` про АМГ-окна и тех.регламент | `_ARCHIVE/5 - документы другого проекта/` |

Наработки 3D-прототипа никуда не делись: они живут в `site/model.js`, `site/odk_frame.js`, `site/textures.js`, `site/hero-scene.html`.

`.gitignore` переписан: раньше в нём были строки, которые ничего не давали (`proto 3d/`, `dist.zip` дважды, `videos/raw_videos/` при уже игнорируемом `videos/`).

---

## Что осталось решить

### 1. Ветки: шесть, нужных две

| Ветка | Последний коммит | Что это |
|---|---|---|
| `hybrid-v8` | 27.07 | **актуальная работа** |
| `gh-pages` | 27.07 | **то, что реально раздаётся** |
| `proto-3d` | 22.07 | отстала на 49 коммитов, целиком входит в `hybrid-v8` |
| `gh-pages-ready` | 22.07 | отстала на 25 коммитов |
| `gh-pages-new` | 22.07 | отстала на 25 коммитов |
| `master` | 04.07 | старый сайт 2026 |

Плюс `origin/HEAD → origin/main`, а `main` содержит один «Initial commit» — поэтому GitHub по умолчанию открывает пустую ветку. Это главная причина ощущения «непонятно, где сайт».

Предложение: закрыть `master`, `proto-3d`, `gh-pages-new`, `gh-pages-ready` тегами `archive/*` и удалить ветки; рабочую ветку сделать веткой по умолчанию на GitHub. Ничего не теряется.

### 2. Деплой: цепочка выяснена, автоматизации нет

Как есть на самом деле (проверено 27.07.2026 в панели хостинга):

```
site/ в hybrid-v8
   │  git archive → пересборка руками
   ▼
gh-pages ──────► GitHub Pages: sudarikovandrey.github.io/playdisplay_web/
   │
   │  git pull по SSH на timeweb
   ▼
~/wordpress_2/public_html  ──────► playdisplay.com  и  playdisplay.ru
```

Корень сайта на хостинге — git-клон репозитория на ветке `gh-pages`. Заливать архивы не нужно, обновление = одна команда:
`cd ~/wordpress_2/public_html && git pull --ff-only origin gh-pages`

`.htaccess` теперь тоже в репозитории (`site/.htaccess`) и приезжает вместе с сайтом.

Что мешает: **`.github/workflows/deploy-pages.yml` ждёт пуша в `proto-3d`** и требует `Settings → Pages → Source: GitHub Actions`. Ни то, ни другое не совпадает с реальностью, поэтому автодеплой не срабатывает, а `gh-pages` собирается руками. Варианты: перевести workflow на рабочую ветку и переключить Source, либо удалить workflow. Отдельно можно автоматизировать и третье звено — cron на хостинге с `git pull` (в панели есть Crontab).

### 2а. Переезд в корень домена — сделано 28.07.2026

Было: в корне `playdisplay.com` стоял WordPress 2021 года, новый сайт прятался в `/new/` под запретом индексации, каноникалы указывали на корень, то есть на чужой сайт.

Стало:

```
~/wordpress_2/
  public_html/          ← новый сайт, git-клон gh-pages, В КОРНЕ домена
    old/                ← статичный слепок старого сайта, noindex
  wp_old_root/          ← сам WordPress, вынесен из веба. База не тронута
```

Проверено снаружи после переезда: главная и страницы кейсов отдаются, `robots.txt` разрешает индексацию, `sitemap.xml` и `data/atlas.json` на месте, `/.git/` закрыт (403), `/projects/`, `/about/`, `/contact/`, `/wp-admin/`, `/xmlrpc.php` редиректят, `/old/` жив. `playdisplay.ru` отдаёт тот же сайт с canonical на `.com`.

Редиректы со старых адресов работ: 7 сменили адрес (`аэропорты-россии` → `airports`, `одк-оак` → `odk-oak`, `ростех-экспо` → `rostec`, `судостроение` → `industry-rf`, `vdnh_space` → `vdnh-space`, `vdnh_space-2` → `vdnh-space-center`, `stallingrad` → `stalingrad`), 8 совпадают, 6 отсутствующих уходят в `/old/work/<slug>/`. Проверено на живых адресах.

Осталось: убрать поддомен `products.playdisplay.com` (сейчас отдаёт новый сайт; по нему расходилась PDF-презентация, так что удаление убьёт внешние ссылки на неё) и продлить домен `playdisplay.com` — оплачен до 18 августа 2026, регистратор внешний.

### 3. `.git` весит 833 МБ

В историю закоммичены видео и гифки:

```
20.0 МБ  assets/GERB_loop.gif
18.5 МБ  dist/videos/ODK.mp4
15.5 МБ  dist/videos/mig.mp4
13.1 МБ  dist/videos/ra.mp4
11.9 МБ  site/assets/concepts/color_ar/Kamchatka.mp4
11.7 МБ  assets/ptk_start_2.gif
11.4 МБ  dist/videos/VDNH_airports.mp4
```

Уборка размер `.git` не уменьшила — блобы остаются в истории. Лечится `git filter-repo` + force-push: отдельная осознанная операция, ломает существующие клоны.

### 4. Разобрать архив

`_ARCHIVE/` весит около 1.2 ГБ. Самый безопасный кандидат на удаление — папка `3 - копии текущего сайта` (490 МБ, уникального содержимого нет). `4 - рабочие материалы` удалять не стоит: там мастера роликов, пережать заново будет неоткуда.
